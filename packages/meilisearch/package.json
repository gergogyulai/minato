{
  "name": "@project-minato/meilisearch",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "meilisearch:start": "docker compose up -d",
    "meilisearch:watch": "docker compose up",
    "meilisearch:stop": "docker compose stop",
    "meilisearch:down": "docker compose down"
  },
  "dependencies": {
    "@project-minato/env": "workspace:*",
    "@project-minato/db": "workspace:*",
    "@elastic/elasticsearch": "^9.2.1",
    "dotenv": "catalog:",
    "meilisearch": "^0.55.0"
  },
  "devDependencies": {
    "@project-minato/config": "workspace:*",
    "typescript": "catalog:"
  }
}
