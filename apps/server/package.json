{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@project-minato/auth": "workspace:*",
    "@project-minato/db": "workspace:*",
    "@project-minato/env": "workspace:*",
    "@project-minato/meilisearch": "workspace:*",
    "@project-minato/queue": "workspace:*",
    "@scalar/hono-api-reference": "^0.9.37",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "^4.8.2",
    "xmlbuilder2": "^4.0.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@project-minato/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
